##### 5.3.3.3 Connectivity

- The documented classes correspond to those that actually exist in the CSS.
- The HTML components use the correct classes and look as intended.
- There are no orphaned styles that are not used anywhere (as far as possible).
