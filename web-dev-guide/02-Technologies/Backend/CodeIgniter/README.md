#### 5.9.3. Quality Control

- [ ] Models use Query Builder (no SQL string concatenation)
- [ ] CSRF protection is enabled in config
- [ ] Views escape output with `htmlspecialchars()` or `<?= esc() ?>`
- [ ] Controllers are thin and delegate to models
