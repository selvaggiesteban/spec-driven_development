# Node.js + Express — Dependencies

**Dimension**: 5 of 7 - Dependencies

## Checklist
- [ ] `package.json` correctly declares dependencies and devDependencies
- [ ] No critical vulnerabilities (`npm audit`)
- [ ] `package-lock.json` is versioned
- [ ] No abandoned packages are used

## Guide
Run `npm audit fix`, update dependencies regularly.

## Control
- [ ] `npm install` works without conflicts
- [ ] No warnings of critical deprecated dependencies
