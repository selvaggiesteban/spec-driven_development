##### 5.5.3.3 Connectivity

- Typical network/external service errors have been tested and the response is controlled.
- If it exposes APIs, they pass basic contract tests (routes, schemas, HTTP codes).
- The app doesn't stop abruptly when external services fail.
