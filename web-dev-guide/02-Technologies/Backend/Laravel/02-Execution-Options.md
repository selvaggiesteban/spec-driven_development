##### 5.8.3.2 Execution Options

- The app starts correctly in local, QA/staging and production with their respective `.env`.
- All installation/configuration commands in the README have been validated on a clean installation.
- Critical `artisan` commands work correctly (migrations, seeds, jobs, etc.).
