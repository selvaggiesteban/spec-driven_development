##### 5.5.3.5 Dependencies

- `pip install -r requirements.txt` / `poetry install` works without conflicts.
- There are no packages marked as insecure or abandoned without a plan.
- The supported Python version is clear and has been tested.
