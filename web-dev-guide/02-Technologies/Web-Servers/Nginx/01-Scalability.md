##### 5.14.1.1 Scalability
- Worker processes configured according to CPUs (`worker_processes auto`)
- Worker connections optimized (`worker_connections 1024`)
- Caching is used (proxy_cache, fastcgi_cache)
- Load balancing configured if applicable
