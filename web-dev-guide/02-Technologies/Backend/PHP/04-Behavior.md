##### 5.1.3.4 Behavior

- Critical flows have automated tests and pass in CI/local.
- Session/authentication management works as expected (expiration, roles).
- Error pages/JSON have consistent format and don't expose sensitive information.
