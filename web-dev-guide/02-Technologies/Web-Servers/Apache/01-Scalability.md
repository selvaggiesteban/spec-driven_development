##### 5.13.1.1 Scalability
- Appropriate MPM is used (prefork, worker, event)
- Connection limits are configured (MaxClients, MaxRequestWorkers)
- Caching is implemented (mod_cache, mod_expires)
- Static resources are served efficiently
