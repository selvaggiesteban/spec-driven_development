##### 5.4.3.5 Dependencies

- `npm install`/`pnpm install`/`yarn install` works in a clean environment.
- There are no unused or obviously duplicated packages.
- Critical vulnerabilities have been reviewed and, if they exist, there is a plan.
