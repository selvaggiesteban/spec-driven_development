##### 5.3.3.5 Dependencies

- Fonts and icons have been tested in environments with resource blockers.
- The system starts even if a CDN is temporarily missing (reasonable fallbacks).
- The declared CSS framework version matches the one actually used.
