##### 5.8.3.6 Deliverable

- Following only the included documentation, a third party can set up the project and run the tests.
- The API endpoints described in the documentation match what the app actually exposes.
- The deployment and rollback procedure has been tested at least once in a non-production environment.
