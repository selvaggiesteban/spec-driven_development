##### 5.1.3.5 Dependencies

- `composer install` works in a clean environment without conflicts.
- There are no abandoned packages or packages with critical vulnerabilities without tracking.
- The app runs without `notice`/`warning`/`deprecated` with high error level.
