##### 5.8.3.5 Dependencies

- `composer install` works without conflicts in a clean environment.
- There are no packages marked as abandoned or with critical vulnerabilities without a mitigation plan.
- The application starts without errors after clearing caches (`config:clear`, `cache:clear`, `route:clear`, `view:clear`).
