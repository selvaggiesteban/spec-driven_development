##### 5.11.1.2 Execution Options
- Environment variables use `REACT_APP_` prefix
- `.env.example` exists with necessary variables
- Production build is optimized
- Scripts in package.json: `start`, `build`, `test`
